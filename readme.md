# Launch

сначала надо поставить `libopencv-dev`, мне уже впадлу было прописывать это в `run.sh`

Запуск:
```bash
sudo ./run.sh
```

# Configuration

хоткей редактируется в [hotkey.py](hotkey.py), по дефолту - `Ctrl + Shift + A`
